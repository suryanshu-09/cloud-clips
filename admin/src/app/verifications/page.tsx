import { VerificationsClient } from "./VerificationsClient";

export const metadata = { title: "Verifications — Cloud Clips Admin" };

export default function VerificationsPage() {
  return <VerificationsClient />;
}
