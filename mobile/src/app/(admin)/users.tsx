import { View, Text } from 'react-native';

/**
 * Admin Users Management Screen (Phase 10.2)
 * Placeholder — full implementation in Phase 10.2
 */
export default function AdminUsersScreen() {
  return (
    <View className="flex-1 bg-gray-50 items-center justify-center p-6">
      <Text className="text-4xl mb-4">👥</Text>
      <Text className="text-xl font-bold text-gray-900 mb-2">User Management</Text>
      <Text className="text-gray-600 text-center">
        Phase 10.2 — User list, barber verification, and user actions.
      </Text>
    </View>
  );
}
