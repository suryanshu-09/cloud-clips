import { View, Text } from 'react-native';

/**
 * Admin Content Management Screen (Phase 10.4)
 * Placeholder — full implementation in Phase 10.4
 */
export default function AdminContentScreen() {
  return (
    <View className="flex-1 bg-gray-50 items-center justify-center p-6">
      <Text className="text-4xl mb-4">📝</Text>
      <Text className="text-xl font-bold text-gray-900 mb-2">Content Management</Text>
      <Text className="text-gray-600 text-center">
        Phase 10.4 — Featured barbers, announcements, product catalog, and support tickets.
      </Text>
    </View>
  );
}
